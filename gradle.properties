# Helps avoid file watcher / snapshot issues on Windows cloud-synced folders (OneDrive/Dropbox)
org.gradle.vfs.watch=false
# This project is small; disabling CC avoids extra cache warnings with some plugin combinations
org.gradle.configuration-cache=false
